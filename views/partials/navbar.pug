.navbar.navbar-default
	.container-fluid
		ul.nav.navbar-nav.navbar-right
			if user
				li 
					a(href='/logout') #{user} is logged in
			else
				li(class={active: page === 'login' })
					a(href='/login') Login
				li(class={active: page === 'register' })
					a(href='/register') Register